# Atlas BI backend dependencies for Windows (64-bit)
# Generated from the Conda environment "BI".
# Recommended runtime: Python 3.12
#
# Install with:
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#
# Only application-level dependencies are pinned here. pip will resolve their
# Windows-compatible transitive dependencies. macOS/Linux-only packages such as
# uvloop and Conda-local file URLs are intentionally excluded.

# Web API
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4

# Database and data processing
SQLAlchemy==2.0.52
PyMySQL==1.2.0
pandas==3.0.5

# LLM, Agent and RAG
langchain==1.3.15
langchain-openai==1.5.1
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
chromadb==1.5.9

# Local embedding model runtime
sentence-transformers==5.7.0
transformers==5.15.0
torch==2.13.0
